FROM node:20-alpine
WORKDIR /app
COPY package.json ./
RUN npm install
COPY . .
ARG FRONTEND_CONTAINER_PORT=5173
ENV VITE_HOST=0.0.0.0 \
    VITE_PORT=${FRONTEND_CONTAINER_PORT}
EXPOSE ${FRONTEND_CONTAINER_PORT}
CMD ["npm", "run", "dev"]
